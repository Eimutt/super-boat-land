using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Items : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
	// Hats: Metal helmet, none(default)
	// Weapons: Sword, axe, club(Default)
	// Boat: Fishing harpoon, huge sail ( improves speed) Steel rudder ( reduces inertia, increases turning speed)
	// Captain: Boots ( improves speed) 
	
    // Update is called once per frame
    void Update()
    {
        
    }
}
